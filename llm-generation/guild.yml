- model: llm
  operations:
    prepare:
      main: prepare
      requires:
        - data
    train:
      main: train
      requires:
        - prepared
    generate:
      main: generate
      requires:
        - trained
    test:
      main: test
      requires:
        - generated

  resources:
    data:
      - file: lyrics-data.csv
      - file: artists-data.csv
    prepared:
      - operation: prepare
    trained:
      - operation: train
    generated:
      - operation: generate